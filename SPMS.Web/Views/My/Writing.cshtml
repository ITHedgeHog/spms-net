@model SPMS.Web.ViewModels.MyWritingViewModel

@{
    ViewData["Title"] = "My writing " + Model.SiteTitle;
}

<div class="row">
    
    <div class="col-10">
        <h1>Draft Posts</h1>
        
        <ul>
            @*@foreach (var character in Model.Characters)
            {
                <li><a asp-action="Details" asp-controller="Biography" asp-area="" asp-route-id="@character.Key">@character.Value</a> [<a asp-action="Edit" asp-controller="Biography" asp-route-id="@character.Key">Edit</a>]</li>
            }*@
            
            @foreach (var post in Model.DraftPosts)
            {
                <li>@post.Title</li>
            }
        </ul>
        
        
        <h1>Unread Posts</h1>
        <ul>

        </ul>
    </div>
    <div class="col-2">
        @if (Model.Characters.Any() && Model.HasEpisode)
        {
            <a asp-action="Post" asp-controller="Authoring" asp-area="" class="btn btn-primary">Create New Post</a>
        }

    </div>
</div>